* {
  border: none;
  border-radius: 0;
  font-family: "JetBrainsMono Nerd Font", "Font Awesome 6 Free", sans-serif;
  font-size: 13px;
  min-height: 0;
}

window#waybar {
  background-color: #1a1b26;
  color: #a9b1d6;
}

/* Workspaces */
#workspaces {
  margin: 0 8px;
  background-color: #16161e;
  padding: 2px;
  border-radius: 12px;
}

#workspaces button {
  padding: 0 12px;
  color: #545c7e;
  background-color: transparent;
  border-radius: 10px;
  margin: 2px;
  transition: all 0.3s ease;
}

#workspaces button:hover {
  background-color: #283457;
  color: #7dcfff;
}

#workspaces button.active {
  background: linear-gradient(135deg, #7aa2f7 0%, #bb9af7 100%);
  color: #1a1b26;
  font-weight: bold;
  box-shadow: 0 0 15px rgba(122, 162, 247, 0.5);
}

#workspaces button.urgent {
  background-color: #f7768e;
  color: #1a1b26;
  animation: urgent-pulse 1.5s ease-in-out infinite;
}

@keyframes urgent-pulse {
  0%, 100% { 
    transform: scale(1);
    box-shadow: 0 0 0 rgba(247, 118, 142, 0.7);
  }
  50% { 
    transform: scale(1.05);
    box-shadow: 0 0 20px rgba(247, 118, 142, 0.7);
  }
}

/* Modules - Style Card avec ombre */
#clock,
#battery,
#cpu,
#memory,
#network,
#pulseaudio,
#bluetooth,
#temperature,
#custom-vpn,
#custom-power {
  padding: 4px 14px;
  margin: 0 4px;
  border-radius: 12px;
  background: linear-gradient(135deg, #24283b 0%, #1f2335 100%);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
  border: 2px solid transparent;
}

#clock {
  background: linear-gradient(135deg, #bb9af7 0%, #9d7cd8 100%);
  color: #1a1b26;
  font-weight: bold;
  font-size: 14px;
  padding: 4px 16px;
  box-shadow: 0 2px 12px rgba(187, 154, 247, 0.4);
}

#battery {
  background: linear-gradient(135deg, #9ece6a 0%, #73daca 100%);
  color: #1a1b26;
  font-weight: bold;
  box-shadow: 0 2px 10px rgba(158, 206, 106, 0.3);
}

#battery.charging {
  animation: charging-glow 2s ease-in-out infinite;
}

@keyframes charging-glow {
  0%, 100% { box-shadow: 0 2px 10px rgba(158, 206, 106, 0.3); }
  50% { box-shadow: 0 2px 20px rgba(158, 206, 106, 0.6); }
}

#battery.warning:not(.charging) {
  background: linear-gradient(135deg, #e0af68 0%, #ff9e64 100%);
}

#battery.critical:not(.charging) {
  background: linear-gradient(135deg, #f7768e 0%, #db4b4b 100%);
  animation: critical-pulse 1s ease-in-out infinite;
}

@keyframes critical-pulse {
  0%, 100% { 
    transform: scale(1);
    box-shadow: 0 2px 10px rgba(247, 118, 142, 0.5);
  }
  50% { 
    transform: scale(1.05);
    box-shadow: 0 2px 20px rgba(247, 118, 142, 0.8);
  }
}

#cpu {
  border-color: #7dcfff;
  color: #7dcfff;
}

#cpu:hover {
  background: linear-gradient(135deg, #7dcfff 0%, #2ac3de 100%);
  color: #1a1b26;
  font-weight: bold;
}

#memory {
  border-color: #bb9af7;
  color: #bb9af7;
}

#memory:hover {
  background: linear-gradient(135deg, #bb9af7 0%, #9d7cd8 100%);
  color: #1a1b26;
  font-weight: bold;
}

#network {
  border-color: #7aa2f7;
  color: #7aa2f7;
}

#network:hover {
  background: linear-gradient(135deg, #7aa2f7 0%, #6183bb 100%);
  color: #1a1b26;
  font-weight: bold;
}

#network.disconnected {
  background: #f7768e;
  color: #1a1b26;
  font-weight: bold;
}

#pulseaudio {
  border-color: #73daca;
  color: #73daca;
}

#pulseaudio:hover {
  background: linear-gradient(135deg, #73daca 0%, #449dab 100%);
  color: #1a1b26;
  font-weight: bold;
}

#pulseaudio.muted {
  background: #414868;
  color: #565f89;
  border-color: #565f89;
}

#bluetooth {
  border-color: #7aa2f7;
  color: #7aa2f7;
}

#bluetooth:hover {
  background: linear-gradient(135deg, #7aa2f7 0%, #6183bb 100%);
  color: #1a1b26;
  font-weight: bold;
}

#bluetooth.disabled {
  background: #414868;
  color: #565f89;
  border-color: #565f89;
}

#temperature {
  border-color: #ff9e64;
  color: #ff9e64;
}

#temperature:hover {
  background: linear-gradient(135deg, #ff9e64 0%, #e0af68 100%);
  color: #1a1b26;
  font-weight: bold;
}

#temperature.critical {
  background: linear-gradient(135deg, #f7768e 0%, #db4b4b 100%);
  color: #1a1b26;
  font-weight: bold;
  animation: critical-pulse 1s ease-in-out infinite;
}

/* VPN */
#custom-vpn {
  border-color: #7aa2f7;
  color: #7aa2f7;
}

#custom-vpn.connected {
  background: linear-gradient(135deg, #9ece6a 0%, #73daca 100%);
  color: #1a1b26;
  font-weight: bold;
  box-shadow: 0 2px 12px rgba(158, 206, 106, 0.4);
}

#custom-vpn.disconnected {
  background: #414868;
  color: #565f89;
  border-color: #565f89;
}

#custom-vpn:hover {
  cursor: pointer;
  transform: scale(1.05);
}

/* Power menu */
#custom-power {
  background: linear-gradient(135deg, #f7768e 0%, #db4b4b 100%);
  color: #1a1b26;
  font-size: 16px;
  font-weight: bold;
  padding: 4px 14px;
  box-shadow: 0 2px 12px rgba(247, 118, 142, 0.4);
}

#custom-power:hover {
  cursor: pointer;
  transform: scale(1.1);
  box-shadow: 0 4px 20px rgba(247, 118, 142, 0.6);
}
